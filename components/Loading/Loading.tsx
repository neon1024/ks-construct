import { Loader, LoadingContainer } from "./styles";

export default function Loading() {
    return (
        <>
            <LoadingContainer>
                <Loader />
            </LoadingContainer>
        </>
    );
}
